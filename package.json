{
  "name": "sherpa-labs",
  "version": "0.0.0",
  "private": true,
  "description": "Sherpa Labs — better rules, smarter agents.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/sherpa-labs-io/SherpaLabs.git"
  },
  "homepage": "https://github.com/sherpa-labs-io/SherpaLabs#readme",
  "bugs": "https://github.com/sherpa-labs-io/SherpaLabs/issues",
  "packageManager": "pnpm@11.1.3",
  "engines": {
    "node": ">=22.22.3",
    "pnpm": ">=11.0.0"
  },
  "workspaces": [
    "apps/*",
    "packages/*",
    "tools/*"
  ],
  "scripts": {
    "build": "tsc --build",
    "build:clean": "tsc --build --clean",
    "typecheck": "tsc --build --dry",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node -e \"console.log('[sherpa-labs] No test suites are wired up yet — placeholder passes so CI stays green. Per-package test scripts will replace this in later phases.');\"",
    "assets": "pnpm -F @sherpa-labs/asset-build build",
    "changeset": "changeset",
    "changeset:status": "changeset status --verbose",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish",
    "prepare": "husky"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.0",
    "@eslint/js": "^9.39.4",
    "@sherpa-labs/shared-config": "workspace:*",
    "eslint": "^9.39.4",
    "eslint-config-prettier": "^9.1.2",
    "globals": "^15.15.0",
    "husky": "^9.1.7",
    "lint-staged": "^15.5.2",
    "prettier": "^3.8.3",
    "typescript": "5.6.3",
    "typescript-eslint": "^8.59.4"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,mjs,cjs}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,mdx,yml,yaml,css}": [
      "prettier --write"
    ]
  }
}
