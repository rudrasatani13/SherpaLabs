# Large Rules

- MUST keep this sentence for token budget testing because it intentionally contains enough words to cross a low configured threshold in a deterministic unit test.
- SHOULD keep adding focused words here so the fixture behaves like a large rule file without needing thousands of tokens in the repository.
- MAY split this guidance later when the token budget rule reports that the configured limit has been exceeded.
