A short rules doc with no headings at all.

- MUST prefer functional components.
- MUST NOT introduce new state libraries without RFC.
- SHOULD use Suspense for data fetching boundaries.
- MAY use Zustand for ephemeral UI state.

Keep diffs small. Reference issue numbers in commit messages.
